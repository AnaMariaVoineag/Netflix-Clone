/* tslint:disable:no-unused-variable */

import { ImagePipe } from './image.pipe';

describe('Pipe: Imagee', () => {
  it('create an instance', () => {
    let pipe = new ImagePipe();
    expect(pipe).toBeTruthy();
  });
});
